import {
  getOrders,
  getOrderByUserId,
  getOrderById,
} from "../services/ordersServices";

const ordersQueries = {
  getOrders,
  getOrderByUserId,
  getOrderById,
};

export default ordersQueries;
